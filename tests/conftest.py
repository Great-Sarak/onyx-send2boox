"""Shared pytest fixtures.

This file is the placeholder for cross-test fixtures. The mock HTTP layer
fixture (responses-based) lands in #2; live-API gating fixtures land in #3.
For Phase 0 #1 (this issue), the file exists but is intentionally empty so
later issues can build on top of it without restructuring."""
